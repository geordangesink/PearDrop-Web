# Changelog

## Unreleased

- Add web peer-status signaling for host visibility (`Joined`, `Downloading`, `Downloaded`, disconnect).
- Add compatibility signaling fields for mixed-version app/web sessions.
- Improve web download flow lifecycle hooks (`complete`/`abort`) for host-side status updates.
- Keep backwards compatibility with older desktop/mobile app versions.
